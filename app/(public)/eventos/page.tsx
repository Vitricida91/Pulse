import { PlaceholderPage } from "@/components/placeholder-page";

export default function EventosPage() {
  return (
    <PlaceholderPage
      eyebrow="Eventos"
      title="Listado de eventos — en construcción"
      description="Acá se mostrarán los shows publicados, con fecha, lugar y precio."
    />
  );
}
