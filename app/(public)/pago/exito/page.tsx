import { PlaceholderPage } from "@/components/placeholder-page";

export default function PagoExitoPage() {
  return (
    <PlaceholderPage
      eyebrow="Pago"
      title="Pago recibido — en construcción"
      description="El estado real de la orden siempre se confirma del lado del servidor, nunca solo por llegar a esta pantalla."
    />
  );
}
